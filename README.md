# gpa-regulat-2026
GPA Batch Repo for Regualr Classes

Here is a comprehensive Python program that accurately calculates a person's age by comparing their birthdate with the current date. It dynamically fetches the current date using the built-in datetime module and accounts for whether the user's birthday has already passed this year.
